# Blocks

Key block:

- `kyber_station` — `KyberStationBlock` used to configure stances/kyber-related UI. Registered via `SaberBlocks`.

Block items are auto-registered alongside blocks and use the mod's item registry.
